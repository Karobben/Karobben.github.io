"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const js_yaml_1 = require("js-yaml");
const path_1 = require("./path");
const fs_1 = require("fs");
function load() {
    const defaultConfig = {
        pattern: '**/*.md',
        ignore: 'node_modules/**/*',
        summary: 'summary.md',
        promote: true,
        template: false,
    };
    let userConfig = js_yaml_1.safeLoad(fs_1.readFileSync(path_1.join(process.cwd(), 'yuque.yml'), 'utf8'));
    const config = Object.assign({}, defaultConfig, userConfig);
    return config;
}
exports.load = load;
