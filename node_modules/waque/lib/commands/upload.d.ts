import { flags } from '@oclif/command';
import LarkClient from '../LarkClient';
import Base from '../base';
export default class Upload extends Base {
    static description: string;
    static flags: {
        watch: import("../../../../../../Users/meck/Workspace/waque-cli/node_modules/@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        token: flags.IOptionFlag<string | undefined>;
    };
    static args: {
        name: string;
    }[];
    static strict: boolean;
    config: any;
    lark: LarkClient;
    pattern?: string;
    run(): Promise<void>;
    updateToc(foundSummary?: string): void;
    isSummary(filename: string): boolean;
    isLayout(filename: string): boolean;
    startWatch(): void;
    handleDoc(filename: string): Promise<void>;
}
