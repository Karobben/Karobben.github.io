import LarkClient from './LarkClient';
export default class User {
    config: any;
    cacheFile: string;
    id?: string;
    token?: string;
    name?: string;
    lark: LarkClient;
    constructor(config: any, token?: string);
    load(): Promise<this>;
    initByToken(save?: boolean): Promise<void>;
    save(user: any): void;
}
